{
  "name": "one-puree-dl",
  "version": "0.1.0",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build src/index.ts --outdir=dist --target=bun"
  },
  "bin": {
    "one-puree-dl": "src/cli.ts"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build src/index.ts --outdir=dist --target=bun",
    "cli": "bun run src/cli.ts"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
